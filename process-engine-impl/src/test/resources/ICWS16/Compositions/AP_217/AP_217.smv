-------------------------------
--   C O M P O S I T I O N   --
-------------------------------
MODULE dn_main

---------------------
--   I N P U T S   --
---------------------
IVAR input:
{
UNDEF, service430_PM_PaymentRequest, service430_PM_ExecutePayment, service430_RESET
};

-----------------------
--   O U T P U T S   --
-----------------------
IVAR output:
{
UNDEF, service430_PM_PaymentReply
};

-------------------------
--   S E R V I C E S   --
-------------------------
--	 service430
VAR service430: {ST2, ST1, ST3, ST0};
ASSIGN
next(service430) :=
case
(service430 = ST2 & output = service430_PM_PaymentReply): ST3;
(service430 = ST0 & input = service430_PM_PaymentRequest): ST1;
(service430 = ST2 & input = service430_RESET): ST0;
(service430 = ST1 & input = service430_RESET): ST0;
(service430 = ST1 & input = service430_PM_ExecutePayment): ST2;
(service430 = ST3 & input = service430_RESET): ST0;
1:service430;
esac;
TRANS ( output = service430_PM_PaymentReply) -> (service430 = ST2)
TRANS ( input = service430_PM_PaymentRequest) -> (service430 = ST0)
TRANS ( input = service430_PM_ExecutePayment) -> (service430 = ST1)
TRANS ( input = service430_RESET) -> (service430 = ST2 | service430 = ST1 | service430 = ST3)

-----------------------
--   O B J E C T S   --
-----------------------
--	 HandlePayment
VAR HandlePayment: {PAYMENT_EXECUTED, INITIAL};
ASSIGN
next(HandlePayment) :=
case
(HandlePayment = PAYMENT_EXECUTED & output = service430_PM_PaymentReply): INITIAL;
(HandlePayment = INITIAL & output = service430_PM_PaymentReply): PAYMENT_EXECUTED;
1:HandlePayment;
esac;
--	 PayPalPayment
VAR PayPalPayment: {INITIAL, PP_PAYMENT_EXECUTED};
ASSIGN
next(PayPalPayment) :=
case
(PayPalPayment = PP_PAYMENT_EXECUTED & input = service430_PM_ExecutePayment): INITIAL;
(input = service430_PM_ExecutePayment):{(PP_PAYMENT_EXECUTED)};
1:PayPalPayment;
esac;
-----------------------------------
--   P R E C O N D I T I O N S   --
-----------------------------------
TRANS (input = service430_PM_PaymentRequest) -> ((((HandlePayment = INITIAL))))
-----------------------------
--   I N V A R I A N T S   --
-----------------------------
--	 Synchronicity
TRANS (1-(input = UNDEF)) + (1-(output = UNDEF)) = 1

---------------------------------------
--   S Y N C H R O N I Z A T I O N   --
---------------------------------------
DEFINE SYNC0 := ((HandlePayment in {PAYMENT_EXECUTED}));
--	 Synchronicity enforcement
TRANS (service430 in {ST2}) -> (next(service430) != service430)
-------------------------------
--   C O N T R O L L E R S   --
-------------------------------
--	 Fault Controller
VAR FAULT_CONTROLLER: { START, STOP };
INIT FAULT_CONTROLLER = START

ASSIGN
next(FAULT_CONTROLLER) := 
case
(FAULT_CONTROLLER = START & FALSE): STOP;
1: FAULT_CONTROLLER;
esac;

TRANS (output != UNDEF | input != UNDEF) -> (FAULT_CONTROLLER != STOP)

-------------------------------------
--   I N I T I A L I Z A T I O N   --
-------------------------------------
INIT ((HandlePayment = INITIAL) & (PayPalPayment = INITIAL))
INIT ((service430 = ST0))

-----------------
--   G O A L   --
-----------------
MODULE main
VAR dn_main : dn_main;

GOAL ONEOF(
(dn_main.SYNC0), 1;
(dn_main.FAULT_CONTROLLER = STOP), 0

)